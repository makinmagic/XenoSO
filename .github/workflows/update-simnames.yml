name: Update Sim Names

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Fetch and update simnames.json
        run: |
          mkdir -p data
          node .github/scripts/update-simnames.js

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/simnames.json
          if ! git diff --cached --quiet; then
            git commit -m "Update simnames.json with new names"
            git push
          else
            echo "No new names found. Skipping commit."
          fi
